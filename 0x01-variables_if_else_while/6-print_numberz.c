#include <stdio.h>

/**
 * main - Prints numbers from 0 to 9 without char or printf functions.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int num;

	for (num = 48; num < 58; num++)
	{
		putchar(i);
	}
	putchar('\n');
	return (0);
}
