#include <stdio.h>

/**
 * main - Prints integers from 0 to 9.
 *
 * Return: Always 0 (Success)
 */
int main()
{
	int x;

	for (x = 0; x < 10; x++)
	{
		printf("%d", x);
	}
	putchar('\n');
	return (0);
}
